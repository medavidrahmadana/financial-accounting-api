[providers]
providers = ["php"]

[variables]
NIXPACKS_PHP_VERSION = "8.2"
NIXPACKS_PHP_EXTENSIONS = "pdo_mysql, gd, zip, mbstring, bcmath"

[phases.build]
cmds = [
  "composer install --ignore-platform-req=ext-gd --no-dev --optimize-autoloader",
  "php artisan config:clear"
]
